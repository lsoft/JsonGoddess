#if !NET5_0_OR_GREATER

namespace System.Runtime.CompilerServices
{
    /// <summary>
    /// Полифилл для <c>init</c>-сеттеров на net472.
    ///
    /// Нужен <b>тестам</b>, а не библиотеке: позиционный <c>record</c>-класс
    /// объявляет свойства с <c>init</c>, а компилятор на каждый такой сеттер
    /// требует этот тип, которого в старом рантайме нет. Тип пустой -
    /// компилятору важно только, что он существует.
    ///
    /// Это не наше ограничение и не наш обход: тот же полифилл вписывает себе
    /// любой, кто пишет <c>record</c> под netstandard2.0. Но знать о нём
    /// потребителю надо, и потому он записан в плане, а не только здесь:
    /// JsonGoddess позиционные записи обслуживает, а вот <b>объявить</b> их на
    /// net472 без этой строчки нельзя.
    /// </summary>
    internal static class IsExternalInit
    {
    }
}

#endif
